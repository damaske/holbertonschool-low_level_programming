#include "main.h"
#include <stdio.h>
/**
 * mul - Multiplies two integers and prints the result
 * @a: First integer
 * @b: Second integer
 * Return: The product of a and b
 */

int mul(int a, int b)
{
	printf("%d\n", a * b);
		return (a * b);
}
